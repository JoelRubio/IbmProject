server:
  port: 8888
  
spring:
  application:
    name: config-server
  profiles:
    active: development
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JoelRubio/IbmProject.git
          clone-on-start: true
          default-label: main