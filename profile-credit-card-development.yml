spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:profileCreditCard
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create

creditCard:
  api:
    name: CREDIT-CARD-WS
    methodGET:
      uri: /{id}
